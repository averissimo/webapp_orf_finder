<%- @results.each do |k, v|  %>
  <div class="mar-l-1em">
    <h3 class="m-t-1em"><%= k.to_s.capitalize %></h3>
    <div class="mar-l-1em">
      <h4>Sequence</h4>
      <div class="mar-l-1em">
        <a class="show-hidden" href="javascript:void(0)">Click here to show sequence</a>
        <div class="hidden">
          <%- @results[k][:sequence].to_s.gsub(/(.{3})/) do |codon| %><span class="codon monospace"><%= codon %></span><%- end  %>
        </div>
      </div>
    </div>
    <%- v.each do |frame, result|  %>
      <%- next if frame == :sequence %>
      <div class="mar-l-1em">
      <h4 <%- if result.empty? %>class="gray no-bold"<%- end %>><%= frame.to_s.capitalize %><%- if result.empty? %><span class="small"> (no result)</span><%- end  %></h4>
        <%- unless result.empty? %>
        <div class="mar-l-1em">
          <!-- Nucleotide sequence -->
          <h5 <%- if result.empty? %>class="gray no-bold"<%- end %>>nt<%- if result.empty? %><span class="small"> (no result)</span><%- end  %></h5>
          <div class="mar-l-1em"><%- result.to_s.gsub(/(.{3})/) do |codon| %><span class="codon monospace"><%= codon %></span><%- end  %></div>
          <!-- Amino-acide sequence -->
          <h5 <%- if result.empty? %>class="gray no-bold"<%- end %>>aa<%- if result.empty? %><span class="small"> (no   result)</span><%- end  %></h5>
          <div class="mar-l-1em"><%- @results_aa[k][frame].to_s.gsub(/(.{1})/) do |codon| %><span class="codon monospace"><%= codon %></span><%- end  %></div>
        </div>
        <%- end  %> <!-- END unless results.empty? -->
      </div>
    <%- end  %> <!-- END v.each -->
  </div>
<%- end  %> <!-- END @results.each -->
